[project]
name = "antimatter-nodes"
version = "0.4.2"
description = "AntiMatter custom nodes for ComfyUI, including ACE-Step Music Director, aspect ratio, proportional image resizing, KREA2 style presets, folder batch loading, prompt loading, and text appending tools."
requires-python = ">=3.10"
dependencies = []
classifiers = [
    "Operating System :: OS Independent",
]

[project.urls]
Repository = "https://github.com/AntiMatterComfy/antimatter-nodes"
Issues = "https://github.com/AntiMatterComfy/antimatter-nodes/issues"

[tool.comfy]
PublisherId = "antimatter"
DisplayName = "AntiMatter Nodes"
