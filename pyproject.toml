[project]
name = "antimatter-nodes"
version = "0.3.0"
description = "AntiMatter custom nodes for ComfyUI, including aspect ratio, image/video loading and saving, LM Studio local API agents, prompt loading, and text appending tools."
requires-python = ">=3.10"
dependencies = []
classifiers = [
    "Operating System :: OS Independent",
]

[project.urls]
Repository = "https://github.com/AntiMatterComfy/antimatter-nodes"
Issues = "https://github.com/AntiMatterComfy/antimatter-nodes/issues"

[tool.comfy]
PublisherId = "antimatter"
DisplayName = "AntiMatter Nodes"
