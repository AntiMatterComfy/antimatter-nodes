# Changelog

## 0.4.1

- Added optional proportional image resizing to `Anti_aspect_ratio_master`.
- Added signed scale controls: `-0.7` retains 70%, `-2` halves an image, and `+2` doubles it.
- Added ComfyUI interpolation method selection and a `resized_image` output.
